#include <stdio.h>
#include "main.h"
/**
 * main - prints its name and new line
 * @argc: number of argumrnts typed
 * @argv: array pointing to arguments
 * Return: 0
*/
int main(int argc, char argv[])
{
    argc = 0;

    printf("%s\n", argv[argc]);
    return (0);
}